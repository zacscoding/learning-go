package channelmulti

/*type Consensus struct {
}

func (c *Consensus) VerifyHeaders(headers []string) (chan<- struct{}, <-chan error) {
	abort := make(chan struct{})
	results := make(chan error, len(headers))
}

func (c *Consensus) verifyHeader(header string) error {
	if len(header) != 5 {
		fmt.Sprintf("")
		return errors.New("header must have 5 length. but ", len(header))
	}
}*/
